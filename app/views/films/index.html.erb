<tr class="table-active">
    <tr class="table-primary">
 
    
     <center>
       <th>Nombre</th>
       <th>Cover</th>
       <th>Año</th>
       <th>Acciones</th>
       </center>
     </tr>
   </thead>
   <tbody>
     <% @films.each do |film| %>
       <tr>
 
   

         <td><%= film.title %></td>         <td><%= film.cover %></td>
        <td>
          <%= image_tag film.cover, height: "120" %>
        </td>
         <td><%= film.year %></td>
         <td><%= link_to "Ver", film_path(film.id), class: "btn btn-outline-dark" %>
           <%= link_to "Editar", edit_film_path(film.id), class: "btn btn-outline-dark" %>
           <%= link_to "Eliminar", film_path(film.id), class: "btn btn-outline-dark", method: :delete %>
 
         </td>
         <td>
         <div class="fixed-bottom">
         
         
           </div>
         </td>
       </tr>
<% end %>