<h1 class="text-center">Pelicula</h1>

<table class="table">
 <tr class="table-active"> </tr>
    <tr class="table-success"> </tr>
    <tr class="table-danger"> </tr>
    <tr class="table-warning"> </tr>

  <thead class="thead-inverse header-table-black">

   
    <th class="table-active">
    <th class="table-success"> Nombre </th>
    <th class="table-danger"> Año </th>
    <th class="table-warning"> Acciones </th>
    
     
  </thead>

  <tbody>

    <% @films.each do |film| %>
      <tr>
        <th class="table-success"><%= film.title %></td>
        <td><%= film.year %></td>
        <td>
          <%= link_to "Ver", film_path(film.id), class: "btn btn-primary" %>
          <%= link_to "Editar", edit_film_path(film.id), class: "btn btn-warning" %>
          <%= link_to "Eliminar", film_path(film.id), class: "btn btn-danger", method: :delete %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to "Nueva Pelicula", new_film_path, class: "btn btn-success" %>